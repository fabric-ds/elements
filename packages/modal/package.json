{
  "name": "@finn-no/fabric-element-modal",
  "version": "0.1.0-alpha.0",
  "description": "Custom elements for fabric",
  "module": "dist/fabric-modal.js",
  "files": [
    "dist/"
  ],
  "types": "dist/fabric-modal.d.ts",
  "scripts": {
    "build": "yarn build:js && yarn build:css",
    "build:js": "tsc && esbuild --format=esm --outfile=dist/fabric-modal.js src/fabric-modal.ts",
    "build:css": "postcss -r dist/fabric-modal.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.schibsted.io/finn/fabric-elements"
  },
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "dom-focus-lock": "^1.0.4",
    "lit-element": "^2.4.0"
  }
}
